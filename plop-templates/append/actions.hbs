  GET_INIT_{{constantCase envKey}}_{{constantCase serviceKey}}_DATA,
  SET_INIT_{{constantCase envKey}}_{{constantCase serviceKey}}_DATA,
  SET_{{constantCase serviceKey}}_{{constantCase envKey}}_RESPONSE,
  NOTIFY_{{constantCase serviceKey}}_{{constantCase envKey}}_CHANGES,